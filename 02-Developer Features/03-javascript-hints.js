// User Guide
// https://github.com/adobe/brackets/wiki/JavaScript-Code-Hints

// DOM
window.document

// jQuery

// Prototype
function MaxDemo() {
}

/**
 * @param {{ state:string }} options
 */
MaxDemo.prototype.sayHello = function (options) {
    return new WebSocket();
};

// Closures
MaxDemo.prototype.send = function (callback) {
    var deferred = new $.Deferred(),
        inClosure = true;
    
    deferred.done(function () {
        var inner = true;
        
    });
};

// Literal
var coffeeCup = {
    status: "empty",
    demo: new MaxDemo()
};

// Assignments
coffeeCup["name"] = "jason";
coffeeCup.size = "small";
